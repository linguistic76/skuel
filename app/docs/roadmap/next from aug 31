● Per the phase directive (stabilize + content — the queue is deferred-work.md § Review Schedule + your rulings, and you decide, I don't self-scope), here's what's actually pending, in the order the memory
  queue holds it:

  Due by the calendar — the September 2026 quarterly review. It's August 31. The standing instruction is to walk every ## section of deferred-work.md against its trigger column (and re-run the left-undone
  census afterward rather than trusting the list). Two rows have their own notes: the Discovery Analytics row says "re-base the number or retire the row" because its 1,000-search-events gate can't fire at the
  observed rate, and planned-ready-aging first fires 2026-09-10.

  Next queued build — the docs updated: auto-stamp. Ruled 2026-08-29; the contract plus its 16 traps are in deferred-work.md § Docs updated: Frontmatter Auto-Stamp. The memory explicitly says build it in a
  fresh context, so that would be a /clear and a new session with that section as the prompt.

  Two rulings only you can make (both flagged "ruling needed"): Principles _validate_update reform-or-delete, and current_streak semantics for habits (#1144).

  The natural continuation of what just shipped — the tool-selection first slice. It's the only thing that lifts the AGGREGATION carve-out, and as of today count questions do classify internally and get
  suppressed (the new log line measures that demand). It's deliberately not scheduled — your call — and it carries two decisions that were split out of PR-2 precisely so they'd get their own attention: coverage
  enforcement (its OPEN PROBLEM 1) and whether an aggregation may enter the Socratic prompt (ADR-077).

  Otherwise: content — leg 3 of the phase.

  My recommendation: the September review first (it's date-triggered and re-prices everything else, including whether the tool slice gets scheduled), then the auto-stamp build in a fresh context. One
  housekeeping note: while answering this I found the "query-stack rulings — add_change_handler wire-or-delete OPEN" queue item was stale — the seam was already deleted on 2026-08-29 and is gone from the tree —
  so I've corrected that memory line; nothing is open there.
