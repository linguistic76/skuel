# Shelved: Curriculum Landing Page

Shelved 2026-03-29. The `/curriculum` hub landing page is replaced by `/profile`
as THE main hub. Profile cards link directly to domain pages (`/lessons`,
`/exercises`, etc.).

The `/curriculum` route now redirects 301 to `/profile`.

## Files
- `landing.py` — `CurriculumLandingView()` 4-card grid with optional StatsGrid
