 I've successfully implemented the BatchOperationHelper DRY consolidation. Here's what was accomplished:

  Files Created

  1. /core/infrastructure/batch/__init__.py - Package initialization
  2. /core/infrastructure/batch/batch_operation_helper.py - Main helper class (~400 lines)
  3. /tests/unit/test_batch_operation_helper.py - Unit tests (19 tests, all passing)

  Files Updated

  Backend/Query Layer:
  - core/models/query/_cypher_generator.py - build_batch_relationship_exists() and build_batch_relationship_count() now delegate to helper
  - adapters/persistence/neo4j/universal_backend.py - get_relationships_batch(), count_relationships_batch(), and delete_relationships_batch() now use helper

  Relationship Services (6 updated):
  - tasks_relationship_service.py:1482 - create_task_relationships() refactored
  - goals_relationship_service.py:1119 - create_goal_relationships() refactored
  - habits_relationship_service.py:898 - create_habit_relationships() refactored
  - events_relationship_service.py:868 - create_event_relationships() refactored
  - choices_relationship_service.py:790 - create_choice_relationships() refactored
  - principles_relationship_service.py:615 - create_principle_relationships() refactored

  Key Methods in BatchOperationHelper

  | Method                                | Purpose                                                       |
  |---------------------------------------|---------------------------------------------------------------|
  | build_relationship_exists_query()     | UNWIND query for batch existence checks                       |
  | build_relationship_count_query()      | UNWIND query for batch counts                                 |
  | build_relationship_properties_query() | Fetch properties for multiple relationships                   |
  | build_relationship_delete_query()     | Batch delete relationships                                    |
  | build_multi_direction_count_queries() | Optimized multi-direction counts                              |
  | build_relationships_list()            | Key for services - Flatten UID lists into relationship tuples |


  Changes Made

  1. GraphQueryExecutor (core/services/graph_query_executor.py:260-360)
    - Added create_relationships_batch() method to enable batch relationship creation for executor-based services
    - This mirrors UniversalNeo4jBackend.create_relationships_batch() for consistency
    - Uses APOC first, falls back to grouped UNWIND queries by relationship type
  2. JournalRelationshipService (core/services/journal_relationship_service.py)
    - Refactored create_journal_relationships() from 2 separate UNWIND queries → single batch call
    - Reduced ~30 lines to ~10 lines
  3. LpRelationshipService (core/services/lp/lp_relationship_service.py)
    - Refactored create_lp_relationships() from 4 separate UNWIND queries → single batch call
    - Reduced ~60 lines to ~15 lines
  4. LsRelationshipService (core/services/ls/ls_relationship_service.py)
    - Refactored create_ls_relationships() from 7 separate UNWIND queries → single batch call
    - Reduced ~110 lines to ~20 lines (biggest win!)
  5. MocRelationshipService (core/services/moc/moc_relationship_service.py)
    - Refactored create_moc_relationships() from 8 separate UNWIND queries → batch calls
    - Handles conditional relationship types (section vs MOC) and cross-domain bridges with properties
    - Reduced ~100 lines to ~40 lines
  6. UserRelationshipService (core/services/user_relationship_service.py)
    - Refactored 3 of 4 relationship types to use batch pattern
    - PINNED relationships kept separate (require indexed order property)
    - Reduced ~45 lines to ~25 lines


  1. Added to BatchOperationHelper (core/infrastructure/batch/batch_operation_helper.py)

  Three new methods for pure Cypher batch relationship creation:

  - group_relationships_by_type() - Groups relationships by type (pure Cypher requires literal relationship types)
  - build_relationship_create_query() - Builds UNWIND query for a single relationship type
  - build_relationship_create_queries() - Main entry point: generates all (query, params) tuples for batch creation

  2. Updated UniversalNeo4jBackend.create_relationships_batch (adapters/persistence/neo4j/universal_backend.py)

  - Before: ~20 lines of inline grouping and query building
  - After: ~10 lines delegating to BatchOperationHelper.build_relationship_create_queries()

  3. Updated GraphQueryExecutor.create_relationships_batch (core/services/graph_query_executor.py)

  - Before: ~20 lines of inline grouping and query building
  - After: ~10 lines delegating to BatchOperationHelper.build_relationship_create_queries()

  DRY Benefits

  | Metric                   | Before                             | After                    |
  |--------------------------|------------------------------------|--------------------------|
  | Duplicate grouping logic | 2 places                           | 1 place (helper)         |
  | Duplicate query building | 2 places                           | 1 place (helper)         |
  | Total lines eliminated   | ~40 lines                          | Centralized in helper    |
  | Consistency              | Slightly different implementations | Identical via delegation |


  Architectural Alignment

  This completes the batch operation DRY consolidation:
  - Pure Cypher - No APOC anywhere in the codebase
  - Single Source of Truth - BatchOperationHelper is the one place for batch query patterns
  - Consistent API - Both UniversalNeo4jBackend and GraphQueryExecutor use identical patterns

