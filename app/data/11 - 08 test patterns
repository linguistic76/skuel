
  💡 Pattern Established

  # Test fixtures can now use:
  @pytest.fixture
  def service_name(mock_backend, mock_driver):
      return ServiceClass(backend=mock_backend, driver=mock_driver)

  # Tests customize behavior:
  def test_something(service_name, mock_backend):
      mock_backend.method.return_value = Result.ok(value)
      result = await service_name.operation()
      assert result.is_ok

