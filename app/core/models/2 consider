-

## What Stays Custom

- `badge_classes.py` — 30+ status/priority/role mappings (SKUEL-specific)
- `RadialProgress` — No MonsterUI equivalent
- `CardGenerator` / `FormGenerator` — Dynamic introspection (targets MonsterUI components now)
- `enum_helpers.py` — Domain presentation logic
- Alpine.js / HTMX — Untouched, MonsterUI doesn't replace these

-

Is there ways to bring the above in lne with the current UI developemnt style?
