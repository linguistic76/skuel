

  I've fully mapped the current architecture. Here's what I found:

  Current Call Chain

  Intelligence Service (e.g., TasksIntelligenceService)
      ↓
  Relationship Service (e.g., TasksRelationshipService)
      ↓
  RelationshipCreationHelper.get_cross_domain_context()
      ↓
  backend.get_task_cross_domain_context()  ← THIS IS IN universal_backend.py!
      ↓
  (~200 lines of Task-specific categorization logic)

  Key Finding: The get_X_cross_domain_context() methods (lines 2413-3770 in universal_backend.py) contain domain-specific intelligence, not universal primitives.

  Phase 2: True Separation of Concerns

  Here's the cleanest architecture that aligns with SKUEL's philosophy:

  ┌─────────────────────────────────────┐
  │  UniversalNeo4jBackend              │
  │  PURE PRIMITIVES (~1500 lines)      │
  │  ----------------------------------- │
  │  ✅ create(), get(), update()       │
  │  ✅ create_relationship()           │
  │  ✅ get_related_uids()              │
  │  ✅ count_related()                 │
  │  ✅ Dynamic protocol via __getattr__│
  │  ❌ NO domain-specific intelligence │
  └──────────────┬──────────────────────┘
                 │ provides primitives
                 ↓
  ┌─────────────────────────────────────┐
  │  RelationshipCreationHelper         │
  │  CROSS-DOMAIN CONTEXT BUILDER       │
  │  ----------------------------------- │
  │  Uses backend primitives to build   │
  │  generic cross-domain context       │
  │  (domain-agnostic graph traversal)  │
  └──────────────┬──────────────────────┘
                 │ provides generic context
                 ↓
  ┌─────────────────────────────────────┐
  │  Domain Intelligence Services       │
  │  DOMAIN-SPECIFIC CATEGORIZATION     │
  │  ----------------------------------- │
  │  TasksIntelligenceService           │
  │    ↳ categorize_task_context()     │
  │      - prerequisites vs dependents  │
  │      - required vs applied knowledge│
  │                                      │
  │  GoalsIntelligenceService           │
  │    ↳ categorize_goal_context()     │
  │      - parent vs subgoals           │
  │      - guiding principles           │
  └─────────────────────────────────────┘

