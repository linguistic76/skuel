# SKUEL secret-scan — ASSIGNMENT-shape catalog.
#
# One credential name per line; `#` comments and blank lines are ignored.
# Read by scripts/git-hooks/secret-scan.sh: a staged line that assigns any name
# below a non-placeholder value is a leak, whatever the value looks like. This
# is how the prefix-free credentials get caught — Deepgram's 40 hex chars, an
# AuraDB password, SESSION_SECRET_KEY.
#
# MIRROR, not a second source of truth: this list must equal
# CredentialSetup.CREDENTIALS.keys() exactly. Bash cannot import Python, so the
# names are duplicated here and pinned by the drift test in
# tests/unit/scripts/test_secret_scan.py — the same arrangement
# scripts/lint_skuel.py::SkuelLinter.CREDENTIAL_CATALOG already uses.
#
NEO4J_PASSWORD
SESSION_SECRET_KEY
OPENAI_API_KEY
ANTHROPIC_API_KEY
HF_API_TOKEN
DEEPGRAM_API_KEY
FIREFLY_APP_KEY
FIREFLY_DB_PASSWORD
FIREFLY_PAT_PERSONAL
FIREFLY_PAT_SKUEL
STRIPE_WEBHOOK_SECRET
RESEND_API_KEY
TEST_ADMIN_PASSWORD
TEST_USER_PASSWORD
