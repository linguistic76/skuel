
● All tests pass. The Option A architecture is working correctly. Let me update the todos and provide a summary:

● Summary: Option A Architecture Implementation Complete

  What Changed

  1. UserContextBuilder now owns user resolution

  # Before: Caller had to fetch user AND pass user_uid (redundant)
  user_result = await self.user_service.get_user(user_uid)
  user = user_result.value
  context_result = await self.context_builder.build_user_context(user_uid, user)

  # After: Builder handles everything internally
  context_result = await self.context_builder.build(user_uid)

  2. UserContextService methods simplified

  Lines eliminated per method:
  - get_context_dashboard(): 13 lines → 4 lines
  - get_context_summary(): 13 lines → 4 lines
  - get_active_context(): 13 lines → 1 line
  - create_tasks_from_goal_context(): 10 lines → 4 lines
  - complete_habit_with_context(): 10 lines → 4 lines

  Total: ~50 lines of repetitive code eliminated

  3. Bootstrap wiring updated

  # Now passes user_service to builder at construction time
  context_builder = UserContextBuilder(driver, user_service=user_service)

  Files Modified

  | File                                       | Changes                                                                           |
  |--------------------------------------------|-----------------------------------------------------------------------------------|
  | core/services/user/user_context_builder.py | Added user_service param, new build() and build_rich() methods                    |
  | core/services/user/user_context_service.py | Simplified all 5 methods to use context_builder.build()                           |
  | core/utils/services_bootstrap.py           | Updated builder creation to pass user_service                                     |
  | core/services/journals_service.py          | Fixed unrelated bug (renamed list() → list_journals() to avoid shadowing builtin) |

  Architecture Benefit

  Before (Crossed Lines):
  UserContextService
      ├── user_service.get_user()  ← repeated in every method
      ├── context_builder.build_user_context(uid, user)  ← both params redundant
      └── actual domain logic

  After (Clean Separation):
  UserContextService
      ├── context_builder.build(uid)  ← builder owns resolution
      └── actual domain logic

  The builder now has clear ownership: "Given a user_uid, I produce a complete UserContext." Consumers don't need to know how users are resolved.

  Nov 27 added

     - get_ready_to_learn() - Knowledge units where ALL prerequisites are mastered
    - get_next_logical_steps() - Natural next steps based on mastered knowledge
    - get_goal_aligned_knowledge() - Knowledge units supporting user's goals
    - get_knowledge_gaps() - Missing prerequisites needed for goals

      2. Updated UserContextService.get_context_dashboard() (user_context_service.py:214-233):
    - Removed driver dependency for predictions
    - Now uses self.intelligence_factory.create(context) to create intelligence
    - Calls intel.get_next_logical_steps() instead of graph_intel


  4. Updated imports and docstrings:
    - Removed import from user_context_service.py
    - Updated module docstrings in user_context_service.py, user_context_builder.py, and __init__.py

  Test Results:
  - 642 integration tests passing
  - 5 pre-existing failures (unrelated to this migration - MEGA-QUERY Cypher syntax issues)
  - All 14 user entity tracking tests pass

  Architecture Now:
  - user_context_builder.py - ALL context building (MEGA-QUERY + graph-sourced)
  - user_context_intelligence.py - ALL intelligence operations (9 methods)
  - user_context_service.py - API orchestration layer (routes)



