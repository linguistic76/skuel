# Local test queries (may contain API keys)
test.cypher

# Credentials
.env

# Neo4j volumes (all 5 mounts from docker-compose.yml — /conf is deliberately not mounted)
neo4j/data/
neo4j/logs/
neo4j/plugins/
neo4j/import/
neo4j/backups/

# Pre-upgrade data archives (backups live outside git — repo is public)
neo4j/*.tgz
