
  UserContextIntelligence has 5 powerful methods that synthesize ALL 13 domain services:

  | Method                                    | What It Does                                       |
  |-------------------------------------------|----------------------------------------------------|
  | get_optimal_next_learning_steps()         | Ranks learning by prerequisites + goals + capacity |
  | get_ready_to_work_on_today()              | Comprehensive daily plan across ALL domains        |
  | get_learning_path_critical_path()         | Fastest route to complete life path                |
  | get_knowledge_application_opportunities() | Maps KU → tasks/habits/goals                       |
  | get_unblocking_priority_order()           | What unlocks the most paths?                       |

    Changes to /core/services/user/unified_user_context.py:

  1. scheduled_event_uids (line 103) - Added to EVENT AWARENESS section
    - Type: list[str]
    - Purpose: All scheduled (future) events for the user
  2. at_risk_goals (line 124) - Added to GOAL AWARENESS section
    - Type: list[str]
    - Purpose: Goals needing attention (used by goals_relationship_service.py)
  3. learning_path_step_uids (line 165) - Added to KNOWLEDGE section
    - Type: list[str]
    - Purpose: Active learning step UIDs (used by ku_graph_service.py)
  4. recently_mastered_uids (line 166) - Added to KNOWLEDGE section
    - Type: set[str]
    - Purpose: Recently mastered KU UIDs for learning momentum calculation (used by user_context_intelligence.py)
  5. pending_choice_uids (line 220) - Added new CHOICE AWARENESS section
    - Type: list[str]
    - Purpose: Choices awaiting decision (used by choices_relationship_service.py)

  Also added bonus fields for completeness:
  - resolved_choice_uids: set of recently resolved choices
  - choice_outcomes: dict mapping choice_uid to outcome

  Result: MyPy errors reduced from 286 → 280 (6 errors fixed), and files with errors reduced from 81 → 79.
