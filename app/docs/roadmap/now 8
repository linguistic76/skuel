
  EXECUTE, no plan file. Implement P1 of the Journals discussion-first arc.
  SoT: docs/roadmap/journals-discussion-first.md (Confirmed — see choices C3, C4, C5 and the P1 row).
  Scope: (1) source panel on the INITIAL journal composer — canon shelf as
  per-book checkboxes (wire through CanonRetrievalService.retrieve's existing
  resource_uids param) + vault toggle; (2) thread the flags through
  /journals/respond → run_standard() and the FOUNDER staged path; (3)
  first-message discussion voice (follow_up_system_prompt family — companion
  voice, user leads, no forced headings). FOUNDER-gate canon/vault server-side
  like /journals/follow-up does. No storage, no ADR changes — that's P2.
  Branch first, commit → PR → Codex, merge when green.

  P2 (the ADR-073 amendment + ConversationSession/ConversationTurn storage) follows once P1 lands — doc-first again, since the amendment needs its own write-up before storage code.
