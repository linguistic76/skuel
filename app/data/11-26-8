
  Can be implemented as simple filter/aggregation:
  - get_active_habits(user_uid) - filter by status != ARCHIVED/COMPLETED
  - get_habit_history(habit_uid) - use completions.get_completions_for_habit()
  - get_completion_calendar(habit_uid) - use completions.get_completion_stats() with date grouping
  - get_event_history(user_uid) - filter by status = COMPLETED + date ordering

  Need actual AI/analytics implementation:
  - get_habit_insights(habit_uid) - needs AI analysis
  - get_habit_recommendations(user_uid) - needs recommendation engine
  - get_optimal_times(habit_uid) - needs time analysis
  - get_event_insights(event_uid) - needs AI analysis
  - get_event_recommendations(user_uid) - needs recommendation engine
  - get_knowledge_insights(uid) - needs AI analysis
  - get_knowledge_recommendations(user_uid) - needs recommendation engine

  Need specific implementation:
  - generate_recurring_series(event_uid) - needs rrule expansion
  - reschedule_event(event_uid, new_date) - needs conflict + cascade
  - bulk_update_knowledge(uids, updates) - needs batch operations


