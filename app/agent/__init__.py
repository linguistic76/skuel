"""SKUEL user-side vault agent (ADR-075 B3).

The runnable program is ``skuel_vault_agent.py`` — a PEP 723 single-file
script. This ``__init__`` exists only so the repo's unit tests can import the
module as ``agent.skuel_vault_agent``.
"""
