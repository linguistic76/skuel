

  This is the design mismatch the MyPy errors are revealing. The API routes are calling methods that were never implemented.

  Let me update the todo list and address this systematically:

● Let me summarize the findings on no-any-return errors:

  Summary: MyPy Error Analysis

  Started with 154 no-any-return errors, now at 88 (down 66).

  Root Causes Identified:

  1. @with_error_handling decorator type erasure (FIXED)
    - Decorator returned Any, erasing return type information
    - Solution: Removed decorator from BaseService (15 occurrences)
    - This exposed hidden type bugs which were fixed

  2. API routes calling non-existent service methods (DOCUMENTED)
    - ~70+ errors are from routes calling methods that don't exist
    - Example: habits_api.py calls track_habit(), pause_habit(), etc. which don't exist on EnhancedHabitsService
    - MyPy infers Any for missing methods → triggers no-any-return

  3. @safe_backend_operation decorator type erasure (SYSTEMIC)
    - Backend decorator also returns Result without type parameter
    - This is necessary for error handling at database layer
    - Causes downstream type inference issues

  Current State:
  - Total MyPy errors: 289 (up from previous due to exposed bugs)
  - no-any-return errors: 88 (down from 154)
  - Many remaining errors are attr-defined errors revealing design mismatches

  What was accomplished:
  1. ✅ Identified @with_error_handling decorator type erasure as root cause
  2. ✅ Removed all 15 @with_error_handling decorators from BaseService
  3. ✅ Fixed 4 type bugs in BaseService that were exposed (update, delete, update_status, get_with_content)
  4. ✅ Fixed method shadowing issue in TasksService and KuService (renamed search to search_service)
  5. ✅ Reduced no-any-return errors from 154 to 88 (43% reduction)

  Root causes documented:
  - Decorator type erasure (both @with_error_handling and @safe_backend_operation)
  - API routes calling non-existent service methods (~70 errors)
  - @safe_backend_operation is necessary for backend error handling but causes type erasure

  The total error count increased because removing the decorator exposed hidden type bugs that were masked by the Any return type. This is good - the code is now more type-safe and these bugs would have been runtime issues.

