# Intent-Traversal ↔ Registry Convergence

**Status:** Phase 0 ✅ done (PR #224, 2026-06-04) — `INTENT_BASED_TRAVERSAL.md` rewritten to reality. **Phase 1 ✅ COMPLETE (One Path Forward, Tasks-first):** PR **2A** ✅ MERGED #225 (engine: `relationship_types` threaded from `UnifiedRelationshipService.get_with_context` → `query_with_intent` → `build_context_query_for_intent`, registry-sourced from `cross_domain_relationship_types` + `LIMIT` cap; **Tasks + Events** routed onto mechanism B; broken Events `get_entity_context(entity_type=...)` `TypeError` deleted) — verified on live Neo4j. PR **2B** ✅ MERGED #226 — **Goals + Habits** routed onto mechanism B by copying the Tasks reference (per-domain `_core_intelligence_mixin` owns `get_with_context` → `self.relationships.get_with_context`; deleted a latent infinite-recursion in each intelligence service's `get_with_context` ↔ `get_<domain>_with_context` delegation) — verified on live Neo4j. PR **2C** — **Choices + Principles** routed onto mechanism B the same way (Choices' own `get_with_context` ↔ `get_choice_with_context` recursion deleted; Principles moved off the inherited EXPLORATORY loader) — verified on live Neo4j (`tests/integration/test_convergence_2c_choices_principles.py`: AFFECTS_GOAL / GUIDES_GOAL surface registry-sourced, missed by the bare HIERARCHICAL clause). **All 6 Activity Domains now retrieve graph context through mechanism B; mechanism A (`GraphContextLoader`) no longer sits on any activity facade/route path** (the sole `self.context_loader` consumer is the shared `_CoreIntelligenceMixin.get_with_context`, overridden in all 6 activity domains). Old Phase 2's "collapse toward one reader" is **folded into Phase 1** — the unification *is* the proof. **Follow-up (curriculum convergence) ✅ COMPLETE:** the 6 activity `get_with_context` overrides were collapsed into the shared `_CoreIntelligenceMixin` (now mechanism B), so Ku/Ps/Lp converge onto B *for free* by inheriting the converted base; `GraphContextLoader` + `SuggestsQueryIntent` + the 3 model `get_suggested_query_intent()` methods + all 9 `_init_context_loader` calls are **deleted** (verified: `tests/integration/test_convergence_curriculum_ku.py`). A latent wiring gap was fixed: `create_lp_sub_services` did not pass `relationship_service`, which the inherited mechanism-B `get_with_context` requires (Ku/Ps already did). **Note on Ku:** its `default_context_intent` is EXPLORATORY (the generic "traverse everything" clause), so convergence *narrows* its generic context to the registry vocabulary — `USES_KU`/`TRAINS_KU` at convergence time; the vocabulary is registry-sourced, so later `KU_CONFIG` additions join automatically (`CITES_RESOURCE` did, via the Resources arc #562–#566) — `ORGANIZES` is intentionally absent from `KU_CONFIG` (Ku hierarchy is served by `get_organization_depth`/`is_organized`). **Phase 2 dead-vocabulary retirement ✅ COMPLETE** (PR 2 of the teardown plan): the three never-selected `QueryIntent` clauses (`PRINCIPLE_EMBODIMENT`/`PRINCIPLE_ALIGNMENT`/`SCHEDULED_ACTION`) + the never-written `CONFLICTS_WITH_GOAL` edge are deleted; live clauses + the registry-sourced path are unaffected. Remaining Phase 2 work (direction-aware bucketing — fold `query_with_intent` onto the `get_cross_domain_context` producer) ✅ MERGED #243 `9b3ab3c2` (flat `graph_context_query_builder.py` deleted) — **Phase 2 COMPLETE**; phased-sketch **Phases 3–4** (uniform `(intent, metrics_fn)` analysis; intents-as-life-questions) not started. **Bespoke PS/LP context-reader cleanup ✅ MERGED #244 `25242f44` (Phase 3 audit-then-decide; Kody + Codex clean first-pass).** B1 liveness audit (re-grep + live-Neo4j proof) found **all four** candidate readers production-DEAD — `/api/{path-steps,pathways}/context` bind to `.intelligence.get_with_context` (inherited `_CoreIntelligenceMixin`, **mechanism B**), proven on live Neo4j to be a *different function* from the bespoke `.core` readers, so nothing live consumed the curated/user-scoped shapes. All four (`ps.core.get_with_context`→`backend.get_step_with_context`, `ps.graph.get_step_with_context`, `lp.core.get_with_context`→`backend.get_path_with_graph_context`, `lp.intelligence.get_path_with_context`→`backend.get_path_with_context`) + their backend Cypher + the `PsStepWithContextRow` family of TypedDicts + protocol decls + dead tests are deleted (One Path Forward). **Design intent preserved:** the deleted curated shapes (knowledge `confidence`, prereq `completed`, habit `current_streak`, the `learning_path` block, per-step `mastery`) had no live consumer; if a rich PS/LP detail-context surface is wanted later, rebuild it on the converged mechanism-B primitive (`build_domain_context_with_paths` edge-property/node-field carry-through), not as a parallel reader.

> **Phase 0 surfaced facts Phase 1 must account for** (verified, in the rewritten doc): intent sourcing is split across **three mechanisms** that facades reach inconsistently — A `GraphContextLoader`→`EXPLORATORY` (Goals/Habits/Choices/Principles facades), B `UnifiedRelationshipService`→config intent (Tasks facade), C `get_entity_context`→`RELATIONSHIP` (Events facade, **currently broken** — passes an unsupported `entity_type=` kwarg → `TypeError`). So Phase 1 must target the specific mechanism each route/facade actually uses, and the broken Events `get_entity_context` call is a latent bug to fix separately.

**Core Principle:** *"One registry, two readers — collapse the duplication, keep the lens."*

## The situation, reframed

SKUEL has **two systems that read the graph around an entity**, grown from opposite ends and now nearly touching:

| | Intent-traversal | Cross-domain-context |
|---|---|---|
| Entry | `GraphIntelligenceService.query_with_intent(domain, uid, intent, depth)` | `UnifiedRelationshipService.get_cross_domain_context(uid, depth, min_confidence)` |
| Edge vocabulary | **Hard-coded** `type(r) IN [...]` per intent in `build_context_query_for_intent` (`adapters/persistence/neo4j/query/graph_context_query_builder.py`) | **Registry-driven** off `DomainRelationshipConfig.cross_domain_relationship_types` |
| Bucketing | none (flat node + rel lists) | incident-edge attribution, `filter_property` tiers, per-`context_field_name` buckets |
| Drift | yes — hand-maintained lists rot (e.g. choice lens names `CONFLICTS_WITH_GOAL`, an edge written nowhere) | no — single source of truth; hardened across #210/#212/#214/#215/#216/#218 |

The audit found the intent side is **mostly aspirational**: every activity-domain model inherits `Entity`'s default `QueryIntent.EXPLORATORY` (no override), so the real intent is whatever the domain *config* sets (`default_context_intent`); `PRINCIPLE_ALIGNMENT` / `PRINCIPLE_EMBODIMENT` / `SCHEDULED_ACTION` are selected by nothing and their query clauses are unreachable; and 5 of the 6 documented per-domain "analysis methods" don't exist.

**The generative read:** the dead `PRINCIPLE_ALIGNMENT` clause is not garbage — it is a *hypothesis* ("to understand a choice's principle-alignment, traverse these edges") that the registry can now answer **correctly**, because #214/#218 established the real choice edges (`AFFECTS_GOAL`, `INFORMED_BY_PRINCIPLE`, `GUIDES_CHOICE`, `INFORMED_BY_KNOWLEDGE`). Point the intent traversal at the registry and the lens comes alive *and* becomes drift-proof — by **removing** duplication, not adding code.

## Destination

> **Intents are named life-questions → resolved through the registry → dispatched through the hardened cross-domain-context machinery → returning one uniform analysis shape.**

- `QueryIntent` stays as the **semantic layer**: a catalog of meaningful questions ("Is this choice aligned with my principles?", "How is this principle lived?", "What does this event execute?"). These are candidate Askesis prompts, ZPD inputs, and UI affordances — not just internal query routing.
- The **edge vocabulary always comes from the registry** (`DomainConfig`), never a hard-coded list. One source of truth feeds both readers; drift becomes structurally impossible.
- Intent differentiates **shape, not vocabulary**: depth emphasis, direction weighting, scoring, which `context_field_name` buckets to foreground — applied on top of the registry edge set.
- Per-domain "analysis" is **one generic method** parameterized by `(intent, metrics_fn)` over `_analyze_entity_with_context`, not six bespoke functions that were never written.

## Phased sketch (each phase independently shippable + verifiable)

**Phase 0 — Honest doc (small, do first regardless). ✅ DONE (PR #224).** Rewrote `INTENT_BASED_TRAVERSAL.md` to *what is actually wired*. Key correction vs. the original belief: intent is **config-sourced, not model-sourced**, but it is reached through **three inconsistent mechanisms** and the config intents barely reach any live caller — so "Goals→GOAL_ACHIEVEMENT / Habits→PRACTICE are the live specialized lenses" was **wrong**. Verified: A `GraphContextLoader`→inherited `EXPLORATORY` (Goals/Habits/Choices/Principles facades); B `UnifiedRelationshipService`→config `default_context_intent` (**only** the Tasks facade reaches this, via `self.relationships.get_with_context` on `TasksService`); C `get_entity_context`→`RELATIONSHIP` (Events facade, **currently broken** — passes an unsupported `entity_type=` kwarg → `TypeError`). Removed the fictional analysis-method table and stale Key-Files paths; marked per-domain specialization *aspirational → this roadmap*.

**Phase 1 — Registry-sourced vocabulary + One Path Forward convergence (the proof). ✅ COMPLETE (PRs #225/#226/2C).** Gave `build_context_query_for_intent` the domain's relationship set (threaded `relationship_types: list[str] | None` from `UnifiedRelationshipService.get_with_context` → `graph_intelligence_service.query_with_intent` → `cross_domain_backend.query_with_intent`, sourced from `config.cross_domain_relationship_types`, **only when `intent is None`** so explicit-intent callers keep their slice). When supplied, the `type(r) IN [...]` filter uses the registry set instead of the hard-coded literal; a `LIMIT` cap was ported into the filtered branch. **The convergence itself was the proof:** rather than thread the new capability into three coexisting mechanisms, all 6 Activity Domains were routed onto **mechanism B** (each per-domain `_core_intelligence_mixin` owns `get_with_context` → `self.relationships.get_with_context`, registry-sourced) — Tasks (reference) + Events in 2A, Goals + Habits in 2B, Choices + Principles in 2C. This **folds in old Phase 2's "collapse toward one reader"**: there is now one activity-domain reader, not a menu. Verified on live Neo4j per PR (the headline win: Choice/Principle traversals surface their real edges, which the bare HIERARCHICAL clause missed entirely). **Activity-path follow-up ✅ DONE (curriculum-convergence teardown):** the 6 activity `get_with_context` overrides were collapsed into the shared `_CoreIntelligenceMixin` (now mechanism B); Ku/Ps/Lp converge by inheriting the converted base; `GraphContextLoader`, `SuggestsQueryIntent`, the 3 model `get_suggested_query_intent()` methods, and all 9 `_init_context_loader` calls are deleted. See the Status block's "Follow-up (curriculum convergence)".

**Phase 2 — Retire the dead intent vocabulary + direction-aware bucketing.** **Dead-vocabulary retirement ✅ COMPLETE (PR 2 of the GraphContextLoader-teardown plan):** the three never-selected `QueryIntent` values (`PRINCIPLE_EMBODIMENT`/`PRINCIPLE_ALIGNMENT`/`SCHEDULED_ACTION`) and their hard-coded per-intent `type(r) IN [...]` clauses in `build_context_query_for_intent` are deleted, along with the never-written `RelationshipName.CONFLICTS_WITH_GOAL` (its only reference was the deleted `PRINCIPLE_ALIGNMENT` clause). **Direction-aware bucketing ✅ MERGED (PR #243 `9b3ab3c2`):** `query_with_intent` no longer runs a parallel flat Cypher path — it folds onto the SAME incident-edge-attributed producer (`build_domain_context_with_paths`) that `get_cross_domain_context` uses, so both graph readers share one Cypher path with strongest-path dedup. `build_context_query_for_intent` (the flat builder) is **deleted**; the four live explicit-intent edge slices (`HIERARCHICAL`/`PREREQUISITE`/`PRACTICE`/`GOAL_ACHIEVEMENT`) were lifted verbatim into `_INTENT_EDGE_SETS` in `cross_domain_backend`, so explicit-intent callers (Tasks deps→PREREQUISITE / practice→PRACTICE) keep their exact edges; a generic intent → all edge types; the registry-sourced (`relationship_types`) path is unchanged. The producer now carries full node `properties` (the `get_entity_context` consumer contract) and keeps the center row when no edge matches (edge-less node → empty context, not NOT_FOUND). `query_with_intent` is now the thin semantic-shaping wrapper this roadmap predicted. Verified live (new `tests/integration/test_direction_aware_bucketing.py`: 2-hop incident attribution + dedup + properties + negative control). **This closes Phase 2.** **Bespoke PS/LP context-reader cleanup ✅ MERGED #244 `25242f44`** — the audit-then-decide found all four readers production-DEAD (routes serve mechanism B, proven on live Neo4j), so the work was entirely dead-reader deletion, not re-platform. See Status block.

**Phase 3 — Uniform analysis via `(intent, metrics_fn)`.** Generalize the per-domain "analysis method" idea onto `_analyze_entity_with_context`: intent selects the lens, registry supplies the edges, the template supplies the fetch→context→metrics→recommendations pipeline, and a domain `metrics_fn` supplies meaning. The existing real analyzers (`analyze_choice_impact`, `get_goal_progress_dashboard`) become instances of one shape rather than divergent one-offs.

> **Audit correction (verified against code):** Phase 3's consolidation was found *already ~80% done* — `_analyze_entity_with_context` exists and Goals/Habits/Principles already run through it with a `metrics_fn`; only the two **Choice** analyzers were divergent one-offs. The deeper finding was *two parallel typed-context families* (UID-only `cross_domain_contexts.py` feeding the template vs. path-aware `path_aware_types.py` feeding the factory), and that the canonical typed reader **`get_cross_domain_context_typed`** was unfinished/unwired (the factory passed config bucket names straight as kwargs → would `TypeError` on any domain whose dataclass fields rename/union the buckets). "intent selects the lens" remains unbuilt and has **no consumer until Phase 4** — deferred there rather than built speculatively.

**Phase 3a — `get_cross_domain_context_typed` as the canonical typed reader (Choices first-mover slice). ✅ DONE (verified on live Neo4j; local — commit pending).** Made `get_cross_domain_context_typed` the One-Path typed reader, proven on Choices. Both Choice analyzers (`analyze_choice_impact` = impact-risk lens, `get_decision_intelligence` = decision-readiness lens) are now thin lenses over a new canonical template `BaseAnalyticsService._analyze_entity_with_typed_context` (shares the fetch prefix with the legacy `_analyze_entity_with_context`; **propagates `Result.fail`** on a real context error instead of silently degrading). The factory's per-domain field gap (select + rename + union + dedup-strongest) is solved by `ChoiceCrossContext.from_categorized` (path_aware_types), which `path_aware_factory.create_cross_context` delegates to when present (other 5 domains untouched, generic path preserved). Choice metrics moved to `metrics_calculators` (`calculate_choice_impact_metrics`/`calculate_decision_metrics` + recommendation fns); `_union_buckets`/`_path_rank` relocated to `path_aware_types` with `PathAware{Goal,Principle,Knowledge}.from_dict`; dead `PathAwareAnalyzer.parse_*` deleted; write-only `ChoiceGraphContext.raw_context` dropped. **Bug fixed in passing:** `ChoiceImpactAnalysis`/`DecisionIntelligence` had no `to_dict()` despite `get_domain_insights` calling it → `GET /api/choices/insights` was a latent 500; added explicit serializers + a unit guard. Verified: `tests/integration/test_cross_domain_context_pipeline.py` (family-B, on live Neo4j — counts/union/dedup-strongest) + `tests/unit/services/choices/test_choices_types_to_dict.py`; `./dev quality` clean. **Full-phase follow-ups ✅ DONE in Phase 3b** (below): the other 5 domains migrated onto `get_cross_domain_context_typed`; the UID-family `cross_domain_contexts.py` + per-type `from_dict`, the dead choice UID-twin (`calculate_choice_metrics` + family-A tests), and the legacy `_analyze_entity_with_context` template are all deleted.

**Phase 3b — Migrate the remaining 5 domains + delete the UID family (full phase). ✅ COMPLETE.** All 6 activity domains now run their cross-domain analysis through the single canonical typed reader `get_cross_domain_context_typed` → `BaseAnalyticsService._analyze_entity_with_typed_context` → per-domain path-aware `*CrossContext.from_categorized` (`core/models/graph/path_aware_types.py`) + path-aware `metrics_fn` in `metrics_calculators.py`. Migration PRs: **#248 Habit, #249 Goal, #250 Principle, #251 Event, #252 Task** (Choices led in Phase 3a). **Cleanup PR (this one):** the dead UID family is deleted — `core/services/intelligence/cross_domain_contexts.py` (7 UID `*CrossContext` dataclasses + `from_dict` + `_uids` helper), the 7 UID `calculate_*_metrics(entity, context)` functions in `metrics_calculators.py` (incl. the choice UID-twin `calculate_choice_metrics`; the path-aware metrics are KEPT), the legacy template `BaseAnalyticsService._analyze_entity_with_context` (+ its `from_dict`-conversion plumbing), and the family-A tests (`tests/test_cross_domain_context_keys.py` + the UID `from_dict` sections of `test_cross_domain_context_pipeline.py`, including the now-moot `test_dependents_bucket_is_dead_for_canonical_depends_on`). NB the name collision: `analytics_service.calculate_task_metrics(user_uid, start, end)` is a different method (analytics metrics service) and is untouched. Deletion + doc-destale only — no behavior change to the live path-aware path. **This closes Phase 3.**

**Phase 4 — Intents as life-questions (product surface).** Expose the intent catalog where it belongs: Askesis reflective prompts, ZPD recommended-action inputs, and detail-page "ask the graph" affordances. This is where the seed flowers — the same machinery that answers an internal query answers a user's question about their own life path.

## Guardrails (lessons already paid for)

- **Don't mechanically patch the doc in isolation** — it describes behavior; a find-replace leaves false claims (see `feedback_mechanical_doc_rename_unsound`). Phase 0 is a genuine rewrite-to-reality, not a rename.
- **Verify every behavioral claim against code or defer it.** Phases 1–2 must be proven on live Neo4j.
- **`CONFLICTS_WITH_GOAL` is written nowhere** — do not resurrect it; if a "conflict" lens is wanted, it needs a real edge a writer produces first.
- **Doc + dead code + plan move together** — pruning the dead `QueryIntent` clauses, correcting the doc, and proving the registry source are one arc, not three drive-by edits.

## Key references

- Audit findings + reframe: memory `project_intent_traversal_registry_convergence`.
- Real choice edges + the cross-domain-context hardening: `project_xdctx_family_a_unimplemented`, PRs #214/#218; `docs/intelligence/CHOICES_INTELLIGENCE.md`.
- Cross-domain-context mechanics (incident-edge attribution, strongest-path dedup): `docs/patterns/UNIFIED_RELATIONSHIP_SERVICE.md`.
- Registry (single source of truth): `core/models/relationship_registry.py` (`DomainRelationshipConfig`, `cross_domain_relationship_types`, `default_context_intent`, `get_intent_for_operation`).
- Live intent engine: `core/services/infrastructure/graph_intelligence_service.py`, `adapters/persistence/neo4j/query/graph_context_query_builder.py`.
